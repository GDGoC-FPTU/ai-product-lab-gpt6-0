# Nhật ký chiêm nghiệm về việc tương tác với AI

Trong suốt buổi học, em sử dụng AI như một trợ lý đồng hành để tăng tốc quá trình suy nghĩ và thử nghiệm ý tưởng. Ở giai đoạn đầu, AI giúp em brainstorm các bài toán vận hành phù hợp với bối cảnh Xanh SM, đặc biệt là các quy trình như điều phối xe, tối ưu sạc và xử lý khiếu nại. Khi bắt đầu làm phần prompt boundary, em tiếp tục dùng AI để gợi ý cách viết system prompt chặt chẽ hơn, tạo ra các test case có tính đối kháng, và hỗ trợ phân tích xem đâu là điểm dễ bị prompt injection hoặc dễ làm mô hình trả lời vượt ranh giới an toàn. Ngoài ra, khi làm với file Python, AI cũng hỗ trợ em đọc yêu cầu đề bài, xác định chỗ cần sửa, và giải thích nguyên nhân của một số lỗi khi chạy code.

Tuy nhiên, AI không phải lúc nào cũng đúng. Một lỗi rõ nhất em gặp là AI đề xuất cách cài đặt phần gọi Gemini theo hướng có fallback sang SDK cũ, trong khi môi trường máy em lại bị chặn `cygrpc`, dẫn đến lỗi DLL và chương trình không chạy được. Ngoài ra, ở phần nội dung, AI đôi lúc có xu hướng đề xuất rule-based quá dài và phức tạp, khiến giải pháp mất tính thực tế cho một bài prototype ngắn. Em cũng nhận ra rằng nếu prompt chưa đủ chặt, AI có thể bị người dùng “dụ” bỏ qua tag `[DRAFT_ONLY]` hoặc vẫn gợi ý phương án không an toàn khi pin xe đang ở mức rất thấp. Điều đó cho thấy AI có thể trả lời nghe hợp lý, nhưng chưa chắc đã đúng với ràng buộc vận hành mà đề bài yêu cầu.

Sau khi phát hiện các vấn đề đó, em đã phải điều chỉnh lại cách làm việc với AI. Thay vì dùng prompt quá mở, em bổ sung các ranh giới rõ ràng ngay trong system prompt, ví dụ yêu cầu mọi phản hồi dạng draft bắt buộc phải bắt đầu bằng `[DRAFT_ONLY]`, và nếu pin dưới 5% thì không được đề xuất trạm sạc xa hơn 5 km mà phải trả về lệnh điều xe sạc di động. Em cũng học được rằng không nên chỉ hỏi AI “hãy viết giúp”, mà cần ép nó tuân thủ bằng các điều kiện có thể kiểm tra được. Khi AI đưa ra hướng xử lý quá phức tạp hoặc không phù hợp với môi trường chạy thực tế, em thu hẹp yêu cầu lại, yêu cầu giải pháp đơn giản hơn, ưu tiên rule rõ ràng, và kiểm tra kết quả bằng các adversarial test thay vì tin ngay vào câu trả lời đầu tiên. Qua buổi học, em thấy AI rất hữu ích khi đóng vai trò thought-partner, nhưng người học vẫn phải giữ vai trò kiểm chứng, đặt ranh giới và sửa sai liên tục thì mới có thể khai thác AI hiệu quả.
